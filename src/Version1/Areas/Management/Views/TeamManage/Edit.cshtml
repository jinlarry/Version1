@model Team

<form class="form-horizontal" asp-area="Management" asp-controller="TeamManage" asp-route-id=@Model.TeamId asp-action="Edit" enctype="multipart/form-data">
    <h4>Edit Team</h4>
    <hr />
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

    <input type="hidden" asp-for="TeamId" />

    <div class="form-group">
        <label asp-for="TeamName" class="col-md-2 control-label">Team Name</label>
        <div class="col-md-10">
            <input asp-for="TeamName" class="form-control" />
            <span asp-validation-for="TeamName" class="text-danger" />
        </div>
    </div>

    <div class="form-group">
        <label   class="col-md-2 control-label"> Team Photo</label>
        <div class="col-md-10">
            <input type="file" id="NewsImagepp" value="Open file" title="Open file" accept=".JPG,.JPEG,.GIF,.PNG,.bmp" name="NewsImagepp" />
            <img id="opop" src="~/images/Teams/@Model.TeamImage" align="left" width="190" height="210" display=inline  >
            <input type="hidden" asp-for="TeamImage" />
        </div>   
            
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Save" class="btn btn-default" />
        </div>
    </div>
</form>

<div>
    <a asp-area="Management" asp-controller="TeamManage" asp-action="Index">Back to List</a>
</div>


@section Scripts {
    <script src="//code.jquery.com/jquery-1.12.4.min.js"></script>
    <script src="//rawgithub.com/indrimuska/jquery-editable-select/master/dist/jquery-editable-select.min.js"></script>
    <script src="/lib/ckeditor/ckeditor.js"></script>
    <link href="//rawgithub.com/indrimuska/jquery-editable-select/master/dist/jquery-editable-select.min.css" rel="stylesheet">


    <script>
    $(function () {
     
   var  getobjectpath= function (file) {
        var url = null;
        if (window.createObjectURL != undefined) {
            url = window.createObjectURL(file)
        } else if (window.URL != undefined) {
            url = window.URL.createObjectURL(file)
        } else if (window.webkitURL != undefined) {
            url = window.webkitURL.createObjectURL(file)
        }
        return url
    };
   $("#NewsImagepp").change(function () {
        var imagepath = getobjectpath(this.files[0]);
        if (imagepath != "") {
            $("#opop").hide;
            $("#opop").attr('src', imagepath);
            $("#opop").fadeIn(1000);
        }
   });
    });
    </script>
}